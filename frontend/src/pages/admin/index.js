import { Box, Typography } from '@mui/material';

const AdminPage = () => {
  return (
    <Box sx={{ mt: 10 }}>
      <Typography variant="h1">Admin Controls will be here</Typography>
    </Box>
  );
};

export default AdminPage;
